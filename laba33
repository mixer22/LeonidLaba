#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    double x,y,z,F;
    cout << "x = ";
    cin >> x;
    cout << "y = ";
    cin >> y;
    cout << "z = ";
    cin >> z;
    F = log(abs((y - sqrt(abs(x))) * (x - (y/(z+pow(x,2))))));
    cout << "F = " << F << endl;
}

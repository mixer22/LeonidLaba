#include <iostream>
using namespace std;

int main() {
    double r,R,l,s,v;
    const double pi = 3.14;
    cout << "R = " << endl;
    cin >> R;
    cout << "r = " << endl;
    cin >> r;
    cout << "l = " << endl;
    cin >> l;
    s = (pi * (R + r) * l) + pi * (R*R) + pi * (r*r);
    v = 0.33 * pi * ((R*R) + (r*r) + (R * r)) * l;
    cout << "S = " << s << endl;
    cout << "V = " << v << endl;
    return 0;
}

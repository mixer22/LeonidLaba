#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    double a, b, p, s;
    cout << "a = ";
    cin >> a;
    cout << "b = ";
    cin >> b;
    p = sqrt(a * a + b * b);
    s = (a + b)/2;
    cout << "P = " << p << endl;
    cout << "S = " << s << endl;
}
